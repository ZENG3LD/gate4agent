# Every cargo build-output directory, not just `target/`: this repo's own
# build tooling routinely points --target-dir at renamed/agent-scoped
# directories (`target-a2`, `target-a2-tui`, `target-monitor-release-v20`,
# `target-task-workbench-*`, ...) both at the repo root and inside the
# nested `crates/gate4agent-tui` workspace. Matches
# `is_skipped_workspace_directory`'s CACHEDIR.TAG-based node-side walk skip
# (crates/gate4agent-node/src/server.rs) in intent, not just by name.
target*/
Cargo.lock
!crates/gate4agent-tui/Cargo.lock

# Local planning and research — not part of the crate release
/docs/plans/
/docs/research/
