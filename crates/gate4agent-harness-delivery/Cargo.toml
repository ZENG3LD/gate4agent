[package]
name = "gate4agent-harness-delivery"
version = "0.2.37"
edition = "2021"
description = "Pure, reviewed-source compiler for staged gate4agent harness delivery bundles (skills, plugins, MCP config, files)"
publish = false

[lib]
name = "gate4agent_harness_delivery"

[dependencies]
gate4agent-node-protocol = { path = "../gate4agent-node-protocol" }
ring = "=0.17.14"
thiserror = "2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Security_Authorization",
    "Win32_Storage_FileSystem",
] }
