[package]
name = "gate4agent-node-wire"
version = "0.2.37"
edition = "2021"
publish = false

[lib]
name = "gate4agent_node_wire"

[dependencies]
gate4agent-node-protocol = { path = "../gate4agent-node-protocol" }
thiserror = "2"
tokio = { version = "1", features = ["io-util", "net", "time"] }

[features]
default = []
fixture = []
