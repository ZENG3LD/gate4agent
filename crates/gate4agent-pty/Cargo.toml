[package]
name = "gate4agent-pty"
version = "0.3.0"
edition = "2021"
description = "In-house PTY backend for gate4agent — Windows ConPTY plus a unix (macOS/Linux) pty backend, std-only, no external dependencies."
license = "MIT"
repository = "https://github.com/ZENG3LD/gate4agent"

[lib]
name = "gate4agent_pty"

[dependencies]
