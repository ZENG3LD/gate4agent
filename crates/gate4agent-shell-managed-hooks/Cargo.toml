[package]
name = "gate4agent-shell-managed-hooks"
version = "0.2.37"
edition = "2021"
description = "Explicit, reversible native authority for gate4agent-managed provider Hook configuration"
publish = false

[dependencies]
gate4agent-adapters = { path = "../gate4agent-adapters" }
gate4agent-shell-hooks = { path = "../gate4agent-shell-hooks" }
gate4agent-types = { path = "../gate4agent-types" }
serde_json = "1"
thiserror = "2"
