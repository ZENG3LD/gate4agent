[package]
name = "gate4agent-testkit"
version = "0.2.37"
edition = "2021"
description = "Authentication-free provider fixtures and the Windows headless test supervisor for gate4agent"
publish = false

[dependencies]
gate4agent-adapters = { path = "../gate4agent-adapters" }
gate4agent-types = { path = "../gate4agent-types" }
