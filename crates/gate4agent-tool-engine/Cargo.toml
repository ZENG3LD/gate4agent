[package]
name = "gate4agent-tool-engine"
version = "0.2.37"
edition = "2021"
description = "Deterministic bounded capability authority engine for gate4agent"
license = "MIT"
publish = false

[lib]
name = "gate4agent_tool_engine"

[dependencies]
gate4agent-tool-protocol = { path = "../gate4agent-tool-protocol" }
gate4agent-types = { path = "../gate4agent-types" }

[dev-dependencies]
serde_json = "1"
